## 分块

下标从1开始

```cpp
sz = sqrt(n); // 块的大小
num = n / sz + (n % sz > 0); // 块的数量
belong[i] = (i - 1) / sz + 1; // i 属于第几块
l[i] = (i - 1) * sz + 1; // 第i块的左端点
r[i] = i * sz; // 第i块的右端点
a[(i - 1) * sz + j]; // 第i块的第j个值在原数组中的位置
```

